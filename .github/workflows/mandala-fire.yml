name: Mandala Fire System

on:
  workflow_dispatch:
    inputs:
      resonance_level:
        description: 'Resonance activation level'
        required: false
        default: 'standard'
        type: choice
        options:
          - standard
          - enhanced
          - transcendent

jobs:
  mandala-activation:
    name: 🔥 Mandala Fire Activation
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Activate Mandala Pulse
        run: |
          echo "🔥 Initiating Mandala Fire System..."
          echo "   Resonance Level: ${{ github.event.inputs.resonance_level }}"
          node .scripts/mandala-pulse.js
          echo "🔥 Mandala Fire System: Activation complete"
