[build]
builder = "DOCKERFILE"
buildCommand = "echo 'Building Quantum Resonance Lattice...'"

[deploy]
numReplicas = 1
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

[experimental]
incrementalBuild = false
