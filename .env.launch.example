# =============================================================================
# 0G ARISTOTLE MAINNET - UNISWAP V2 DEPLOYMENT CONFIGURATION
# =============================================================================
# This file contains deployed contract addresses and network configuration
# Copy this file to .env.launch and fill in actual addresses after deployment
# =============================================================================

# Network Configuration
ZERO_G_CHAIN_ID=16661
ZERO_G_RPC_URL=https://evmrpc.0g.ai
ZERO_G_BLOCK_EXPLORER=https://chainscan.0g.ai

# Deployed Contract Addresses (Fill after deployment)
ZERO_G_W0G=<deployed_w0g_address>
ZERO_G_WETH=<deployed_w0g_address>  # Alias for compatibility
ZERO_G_FACTORY=<deployed_factory_address>
ZERO_G_UNIVERSAL_ROUTER=<deployed_router_address>

# Deployment Metadata
ROUTER_SOURCE=self_deployed
ROUTER_DEPLOYED_BY=onenoly1010
ROUTER_TYPE=uniswap_v2
ROUTER_DEPLOYMENT_DATE=2025-12-19
ROUTER_DEPLOYMENT_TX=<router_deployment_tx_hash>

# Additional Deployment Info
W0G_DEPLOYMENT_TX=<w0g_tx_hash>
FACTORY_DEPLOYMENT_TX=<factory_tx_hash>
PAIR_INIT_CODE_HASH=<pair_init_code_hash>

# =============================================================================
# TESTNET CONFIGURATION (Chain ID: 5611)
# =============================================================================
# Uncomment and use for testnet deployment

# ZERO_G_CHAIN_ID=5611
# ZERO_G_RPC_URL=https://evmrpc-testnet.0g.ai
# ZERO_G_BLOCK_EXPLORER=https://testnet.0g.ai

# =============================================================================
# INTEGRATION SETTINGS
# =============================================================================

# Gas Configuration
MAX_GAS_PRICE=100  # Maximum acceptable gas price in gwei
GAS_LIMIT_SWAP=200000
GAS_LIMIT_ADD_LIQUIDITY=300000

# Slippage Tolerance (basis points)
DEFAULT_SLIPPAGE_BP=50  # 0.5%
MAX_SLIPPAGE_BP=1000    # 10%

# Transaction Deadline (seconds from now)
DEFAULT_DEADLINE=1200  # 20 minutes

# =============================================================================
# SECURITY & MONITORING
# =============================================================================

# Fee Recipient (feeToSetter address)
FEE_TO_SETTER=<multisig_address>  # Should be multisig in production

# Monitoring
ENABLE_TX_MONITORING=true
ALERT_WEBHOOK_URL=<your_alert_webhook>

# Rate Limiting
MAX_REQUESTS_PER_MINUTE=60

# =============================================================================
# USAGE INSTRUCTIONS
# =============================================================================
#
# 1. After deploying contracts, copy addresses to this file
# 2. Rename to .env.launch (without .example)
# 3. Add to .gitignore to prevent committing sensitive data
# 4. Source in application:
#    - Backend: Load in server/config.py
#    - Frontend: Use in swap interface
#
# 5. Verify deployment:
#    python scripts/verify_0g_dex.py
#
# 6. Test integration:
#    - Wrap 0.01 0G to W0G
#    - Create test pair
#    - Add liquidity
#    - Execute test swap
#
# =============================================================================
